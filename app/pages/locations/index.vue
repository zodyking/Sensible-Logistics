<script setup lang="ts">
/**
 * Locations live on the Containers tab (design template: locations ▸ yard ▸ history).
 */
definePageMeta({
  middleware: () => navigateTo('/containers', { replace: true }),
})
</script>

<template>
  <div />
</template>
